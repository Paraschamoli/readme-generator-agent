# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenRouter API Configuration (Alternative to OpenAI)
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# GitHub API Configuration (Required for repository access)
# Get your personal access token from: https://github.com/settings/tokens
GITHUB_ACCESS_TOKEN=your_github_personal_access_token_here

# Mem0 Memory Configuration (Optional - for memory features)
# Get your API key from: https://app.mem0.ai/dashboard/api-keys
MEM0_API_KEY=your_mem0_api_key_here

# Model Configuration (Optional - defaults to OpenAI GPT-4o)
# MODEL_NAME=openai/gpt-4o

# Debug Configuration (Optional)
# DEBUG=true
# DEBUG_LEVEL=2

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Choose at least one provider and set its API key:
#    - Set OPENAI_API_KEY for OpenAI GPT models
#    - OR set OPENROUTER_API_KEY for OpenRouter access (supports multiple models)
# 3. REQUIRED: Set GITHUB_ACCESS_TOKEN for GitHub repository access
# 4. Optionally set MEM0_API_KEY for memory and context features
# 5. The agent will work with just OpenAI/OpenRouter + GitHub token

# GitHub Token Setup:
# 1. Go to https://github.com/settings/tokens
# 2. Generate new token with "repo" scope (full control of private repositories)
# 3. For public repos only, use "public_repo" scope

# Note: The agent supports multiple API providers in this priority order:
# 1. OpenAI API (if OPENAI_API_KEY is set)
# 2. OpenRouter API (if OPENROUTER_API_KEY is set)
# 3. If neither is set, the agent will fail with a clear error message

# For GitHub access: Without GITHUB_ACCESS_TOKEN, repository analysis is disabled
# but file system tools still work for basic README generation